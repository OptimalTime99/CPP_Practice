class ShapeManager {
public:
    void drawShape(int shapeType) {
        if (shapeType == 1) {
            // 원 그리기
        }
        else if (shapeType == 2) {
            // 사각형 그리기
        }
    }
};

// 다른 if문은 건드리더라도 수정이 열려있는 것으로 본다.